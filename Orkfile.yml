global:
  default: build

tasks:
  - name: build
    description: build the application
    env:
      GOOS: linux
      GOARCH: amd64
      GO_TARGET: bin/ork
    actions:
      - go build -o $GO_TARGET

  - name: test
    description: test the application
    depends_on:
      - build
    actions:
      - go test . -v -count=1

  - name: clean
    actions:
      - rm -rf bin
